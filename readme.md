
```shell
git init
git config user.name "dyxs-site"
git config user.email "mail@dyxs.site"
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/dyxs-site/docs-seo.git
git push -u origin main
```
